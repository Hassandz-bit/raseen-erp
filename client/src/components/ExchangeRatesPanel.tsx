import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip";
import { useLanguage } from "@/contexts/LanguageContext";
import { formatOrganizationDate, formatOrganizationNumber } from "@/lib/formatting";
import { trpc } from "@/lib/trpc";
import { FileSpreadsheet, FileText, Loader2, Plus, RefreshCw } from "lucide-react";
import { useState } from "react";
import { PDFDocument, StandardFonts, rgb } from "pdf-lib";
import { toast } from "sonner";

function downloadFile(name: string, type: string, content: string) {
  const url = URL.createObjectURL(new Blob([content], { type }));
  const anchor = document.createElement("a");
  anchor.href = url; anchor.download = name; anchor.click();
  URL.revokeObjectURL(url);
}

export function ExchangeRatesPanel() {
  const { language, formatSettings } = useLanguage();
  const [baseCurrencyCode, setBaseCurrencyCode] = useState("DZD");
  const [quoteCurrencyCode, setQuoteCurrencyCode] = useState("EUR");
  const [rate, setRate] = useState("");
  const rates = trpc.erp.preferences.exchangeRates.useQuery();
  const addRate = trpc.erp.preferences.addExchangeRate.useMutation({ onSuccess: () => { toast.success(language === "ar" ? "تمت إضافة سعر الصرف" : language === "fr" ? "Taux de change ajouté" : "Exchange rate added"); setRate(""); rates.refetch(); }, onError: error => toast.error(error.message) });
  const exportExcel = () => {
    const rows = rates.data ?? [];
    const header = "Base\tQuote\tRate\tEffective date\tSource";
    const values = rows.map(row => `${row.baseCurrencyCode}\t${row.quoteCurrencyCode}\t${row.rate}\t${new Date(row.effectiveAt).toISOString()}\t${row.source}`);
    downloadFile("nawa-exchange-rates.xls", "application/vnd.ms-excel;charset=utf-8", [header, ...values].join("\n"));
  };
  const exportPdf = async () => {
    const rows = rates.data ?? [];
    const pdf = await PDFDocument.create(); const font = await pdf.embedFont(StandardFonts.Helvetica); const bold = await pdf.embedFont(StandardFonts.HelveticaBold); const rowsPerPage = 25;
    rows.forEach((row, index) => { const position = index % rowsPerPage; const pageIndex = Math.floor(index / rowsPerPage); if (position === 0) { const page = pdf.addPage([842, 595]); page.drawText("Nawa ERP — Exchange rate history", { x: 42, y: 550, size: 18, font: bold, color: rgb(0.12, 0.16, 0.22) }); page.drawText(`Page ${pageIndex + 1}`, { x: 755, y: 550, size: 9, font }); page.drawText("Base     Quote     Rate                 Effective date             Source", { x: 42, y: 520, size: 10, font: bold }); } const page = pdf.getPages()[pageIndex]; page.drawText(`${row.baseCurrencyCode.padEnd(9)}${row.quoteCurrencyCode.padEnd(10)}${String(row.rate).padEnd(21)}${new Date(row.effectiveAt).toISOString().slice(0, 10).padEnd(27)}${row.source}`, { x: 42, y: 500 - position * 17, size: 9, font }); });
    const bytes = await pdf.save(); const buffer = bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength) as ArrayBuffer; const url = URL.createObjectURL(new Blob([buffer], { type: "application/pdf" })); const anchor = document.createElement("a"); anchor.href = url; anchor.download = "nawa-exchange-rates.pdf"; anchor.click(); URL.revokeObjectURL(url);
  };
  const add = () => { const parsed = Number(rate); if (!Number.isFinite(parsed) || parsed <= 0) return toast.error(language === "ar" ? "أدخل سعراً صحيحاً" : "Enter a valid rate"); addRate.mutate({ baseCurrencyCode: baseCurrencyCode.toUpperCase(), quoteCurrencyCode: quoteCurrencyCode.toUpperCase(), rate: parsed, effectiveAt: new Date(), source: "manual" }); };
  const text = language === "ar" ? { title: "سجل أسعار الصرف", description: "سجل يدوي تاريخي؛ لا يغيّر أسعار المعاملات السابقة.", add: "إضافة سعر", base: "الأساس", quote: "الاقتباس", rate: "السعر", excel: "Excel", pdf: "PDF" } : language === "fr" ? { title: "Historique des taux", description: "Historique manuel; les transactions passées ne sont jamais modifiées.", add: "Ajouter", base: "Base", quote: "Contrepartie", rate: "Taux", excel: "Excel", pdf: "PDF" } : { title: "Exchange rate history", description: "Manual historical ledger; it never changes past transaction rates.", add: "Add rate", base: "Base", quote: "Quote", rate: "Rate", excel: "Excel", pdf: "PDF" };
  return <section className="surface rounded-3xl border p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-xl font-bold text-white">{text.title}</h2><p className="mt-2 text-sm text-muted-foreground">{text.description}</p></div><div className="flex gap-2"><Tooltip><TooltipTrigger asChild><Button variant="outline" onClick={exportExcel} disabled={!rates.data?.length} className="border-white/10 bg-white/[.03] text-slate-200"><FileSpreadsheet className="me-2 h-4 w-4" />{text.excel}</Button></TooltipTrigger><TooltipContent>Export .xls</TooltipContent></Tooltip><Tooltip><TooltipTrigger asChild><Button variant="outline" onClick={exportPdf} disabled={!rates.data?.length} className="border-white/10 bg-white/[.03] text-slate-200"><FileText className="me-2 h-4 w-4" />{text.pdf}</Button></TooltipTrigger><TooltipContent>Download PDF</TooltipContent></Tooltip></div></div><div className="mt-6 grid gap-3 md:grid-cols-[1fr_1fr_1fr_auto]"><label className="text-xs text-muted-foreground">{text.base}<Input value={baseCurrencyCode} maxLength={3} onChange={event => setBaseCurrencyCode(event.target.value)} className="mt-2 h-10 border-white/10 bg-white/[.04] text-white" /></label><label className="text-xs text-muted-foreground">{text.quote}<Input value={quoteCurrencyCode} maxLength={3} onChange={event => setQuoteCurrencyCode(event.target.value)} className="mt-2 h-10 border-white/10 bg-white/[.04] text-white" /></label><label className="text-xs text-muted-foreground">{text.rate}<Input type="number" min="0" step="0.00000001" value={rate} onChange={event => setRate(event.target.value)} className="mt-2 h-10 border-white/10 bg-white/[.04] text-white" /></label><Tooltip><TooltipTrigger asChild><Button onClick={add} disabled={addRate.isPending} className="mt-5 h-10 bg-primary text-primary-foreground">{addRate.isPending ? <Loader2 className="h-4 w-4 animate-spin" /> : <Plus className="h-4 w-4" />}</Button></TooltipTrigger><TooltipContent>{text.add}</TooltipContent></Tooltip></div>{rates.isLoading ? <div className="mt-8 flex items-center gap-2 text-sm text-muted-foreground"><RefreshCw className="h-4 w-4 animate-spin text-primary" />{language === "ar" ? "جارٍ تحديث الأسعار…" : language === "fr" ? "Mise à jour des taux…" : "Refreshing rates…"}</div> : rates.isError ? <div className="mt-8 rounded-2xl border border-rose-400/20 bg-rose-400/10 p-4 text-sm text-rose-200"><p>{language === "ar" ? "تعذر تحميل سجل أسعار الصرف." : language === "fr" ? "Impossible de charger l’historique." : "Could not load exchange-rate history."}</p><Button onClick={() => rates.refetch()} variant="outline" className="mt-3 border-rose-300/30 bg-transparent text-rose-100">{language === "ar" ? "إعادة المحاولة" : language === "fr" ? "Réessayer" : "Retry"}</Button></div> : <div className="thin-scrollbar mt-6 overflow-x-auto"><table className="w-full min-w-[620px] text-start text-sm"><thead className="border-b border-white/10 text-muted-foreground"><tr><th className="p-3">{text.base}</th><th className="p-3">{text.quote}</th><th className="p-3">{text.rate}</th><th className="p-3">{language === "ar" ? "تاريخ السريان" : language === "fr" ? "Date d’effet" : "Effective date"}</th><th className="p-3">{language === "ar" ? "المصدر" : language === "fr" ? "Source" : "Source"}</th></tr></thead><tbody>{rates.data?.map(row => <tr key={row.id} className="border-b border-white/[.06]"><td className="p-3 text-white">{row.baseCurrencyCode}</td><td className="p-3 text-white">{row.quoteCurrencyCode}</td><td className="p-3 text-primary">{formatOrganizationNumber(Number(row.rate), { ...formatSettings, decimalPlaces: 8 })}</td><td className="p-3 text-slate-300">{formatOrganizationDate(row.effectiveAt, formatSettings)}</td><td className="p-3 text-slate-300">{row.source}</td></tr>)}{rates.data?.length === 0 && <tr><td colSpan={5} className="p-7 text-center text-muted-foreground">{language === "ar" ? "لا توجد أسعار صرف مسجلة" : language === "fr" ? "Aucun taux enregistré" : "No exchange rates recorded"}</td></tr>}</tbody></table></div>}</section>;
}
