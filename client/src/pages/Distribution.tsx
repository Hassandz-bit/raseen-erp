import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Skeleton } from "@/components/ui/skeleton";
import { useLanguage } from "@/contexts/LanguageContext";
import { downloadDistributionExcel, downloadDistributionPdf } from "@/lib/distributionReportExport";
import { trpc } from "@/lib/trpc";
import { Activity, CircleAlert, Download, FileText, MapPinned, RefreshCw, Route, Smartphone, Truck, Wallet } from "lucide-react";
import React, { useState } from "react";
import { toast } from "sonner";

const copy = {
  ar: { eyebrow: "مركز عمليات التوزيع", title: "الأسطول والجولات في رؤية واحدة", description: "تابع المسارات النشطة والتحميلات والتسليمات والتحصيل على بيانات مؤسستك فقط.", refresh: "تحديث البيانات", driverWorkspace: "واجهة السائق", exportPdf: "تصدير PDF", exportExcel: "تصدير Excel", exportReady: "تم تجهيز التقرير للتنزيل.", exportFailed: "تعذر تصدير التقرير حالياً.", newVehicle: "مركبة جديدة", newTerritory: "منطقة جديدة", newRoute: "جولة جديدة", create: "إنشاء", vehicleCode: "رمز المركبة", registration: "رقم التسجيل", vehicleType: "نوع المركبة", payload: "الحمولة القصوى", volume: "الحجم الأقصى", territoryCode: "رمز المنطقة", territoryName: "اسم المنطقة", routeDateInput: "تاريخ الجولة", created: "تم إنشاء السجل وتحديث المركز.", createFailed: "تعذر إنشاء السجل. تحقق من الصلاحيات والبيانات.", routesToday: "جولات اليوم", activeRoutes: "جولات نشطة", loadedVehicles: "مركبات محملة", pendingDeliveries: "تسليمات معلقة", collections: "تحصيلات مسجلة", returns: "مرتجعات مسجلة", capacity: "متوسط استغلال الحمولة", operations: "الجولات الأخيرة", fleet: "الأسطول", territories: "المناطق", noRoutes: "لا توجد جولات تشغيلية بعد.", noVehicles: "لا توجد مركبات مسجلة بعد.", noTerritories: "لا توجد مناطق توزيع بعد.", vehicle: "المركبة", route: "الجولة", status: "الحالة", routeDate: "التاريخ", mobileWarehouse: "المخزن المتنقل", capacityText: "الحمولة", error: "تعذر تحميل بيانات التوزيع. تحقق من الاشتراك والصلاحيات.", settings: "سياسة الحمولة", hardBlock: "منع صارم", warning: "تنبيه", managerOverride: "تجاوز إداري" },
  fr: { eyebrow: "Centre des opérations", title: "Flotte et tournées, dans une vue unique", description: "Suivez les itinéraires, chargements, livraisons et encaissements de votre organisation.", refresh: "Actualiser", driverWorkspace: "Espace conducteur", exportPdf: "Exporter PDF", exportExcel: "Exporter Excel", exportReady: "Le rapport est prêt au téléchargement.", exportFailed: "Impossible d’exporter le rapport.", newVehicle: "Nouveau véhicule", newTerritory: "Nouvelle zone", newRoute: "Nouvelle tournée", create: "Créer", vehicleCode: "Code véhicule", registration: "Immatriculation", vehicleType: "Type de véhicule", payload: "Charge maximale", volume: "Volume maximal", territoryCode: "Code zone", territoryName: "Nom de la zone", routeDateInput: "Date de tournée", created: "L’enregistrement a été créé et le centre est actualisé.", createFailed: "Impossible de créer l’enregistrement. Vérifiez les permissions et les données.", routesToday: "Tournées du jour", activeRoutes: "Tournées actives", loadedVehicles: "Véhicules chargés", pendingDeliveries: "Livraisons en attente", collections: "Encaissements", returns: "Retours", capacity: "Utilisation moyenne", operations: "Dernières tournées", fleet: "Flotte", territories: "Zones", noRoutes: "Aucune tournée opérationnelle.", noVehicles: "Aucun véhicule enregistré.", noTerritories: "Aucune zone de distribution.", vehicle: "Véhicule", route: "Tournée", status: "Statut", routeDate: "Date", mobileWarehouse: "Entrepôt mobile", capacityText: "Charge", error: "Impossible de charger les données de distribution. Vérifiez l’abonnement et les permissions.", settings: "Politique de charge", hardBlock: "Blocage strict", warning: "Avertissement", managerOverride: "Dérogation manager" },
  en: { eyebrow: "Distribution operations", title: "Fleet and routes in one view", description: "Monitor routes, loading, deliveries, and collections using organization-scoped data.", refresh: "Refresh data", driverWorkspace: "Driver workspace", exportPdf: "Export PDF", exportExcel: "Export Excel", exportReady: "The report is ready to download.", exportFailed: "The report could not be exported.", newVehicle: "New vehicle", newTerritory: "New territory", newRoute: "New route", create: "Create", vehicleCode: "Vehicle code", registration: "Registration", vehicleType: "Vehicle type", payload: "Maximum payload", volume: "Maximum volume", territoryCode: "Territory code", territoryName: "Territory name", routeDateInput: "Route date", created: "The record was created and the center was refreshed.", createFailed: "The record could not be created. Verify permissions and data.", routesToday: "Today’s routes", activeRoutes: "Active routes", loadedVehicles: "Loaded vehicles", pendingDeliveries: "Pending deliveries", collections: "Collections", returns: "Returns", capacity: "Average capacity use", operations: "Recent routes", fleet: "Fleet", territories: "Territories", noRoutes: "No operational routes yet.", noVehicles: "No vehicles have been registered yet.", noTerritories: "No distribution territories yet.", vehicle: "Vehicle", route: "Route", status: "Status", routeDate: "Date", mobileWarehouse: "Mobile warehouse", capacityText: "Load", error: "Distribution data could not be loaded. Verify subscription and permissions.", settings: "Load policy", hardBlock: "Hard block", warning: "Warning", managerOverride: "Manager override" },
} as const;

const statusStyle: Record<string, "default" | "secondary" | "destructive" | "outline"> = { planned: "outline", prepared: "secondary", loaded: "secondary", started: "default", in_progress: "default", returning: "secondary", closing: "secondary", closed: "outline", cancelled: "destructive", active: "default", maintenance: "destructive", inactive: "outline" };

function Metric({ title, value, icon: Icon, accent = "text-primary" }: { title: string; value: string; icon: typeof Route; accent?: string }) {
  return <Card className="border-border/70 bg-card/70 shadow-sm"><CardContent className="flex items-center justify-between p-5"><div><p className="text-sm text-muted-foreground">{title}</p><p className="mt-2 text-2xl font-bold tabular-nums">{value}</p></div><span className={`rounded-xl bg-muted p-3 ${accent}`}><Icon className="h-5 w-5" /></span></CardContent></Card>;
}

export default function Distribution() {
  const { language, direction, formatCurrency, formatDate, formatNumber, formatPercentage } = useLanguage();
  const text = copy[language];
  const control = trpc.erp.distribution.controlCenter.useQuery();
  const routes = trpc.erp.distribution.routes.list.useQuery();
  const vehicles = trpc.erp.distribution.vehicles.list.useQuery();
  const territories = trpc.erp.distribution.territories.list.useQuery();
  const settings = trpc.erp.distribution.settings.get.useQuery();
  const [exporting, setExporting] = useState<"pdf" | "excel" | null>(null);
  const [vehicleOpen, setVehicleOpen] = useState(false);
  const [territoryOpen, setTerritoryOpen] = useState(false);
  const [routeOpen, setRouteOpen] = useState(false);
  const createVehicle = trpc.erp.distribution.vehicles.create.useMutation({ onSuccess: () => { setVehicleOpen(false); refresh(); toast.success(text.created); }, onError: () => toast.error(text.createFailed) });
  const createTerritory = trpc.erp.distribution.territories.create.useMutation({ onSuccess: () => { setTerritoryOpen(false); refresh(); toast.success(text.created); }, onError: () => toast.error(text.createFailed) });
  const createRoute = trpc.erp.distribution.routes.create.useMutation({ onSuccess: () => { setRouteOpen(false); refresh(); toast.success(text.created); }, onError: () => toast.error(text.createFailed) });
  const refresh = () => { void control.refetch(); void routes.refetch(); void vehicles.refetch(); void territories.refetch(); void settings.refetch(); };
  const loading = control.isLoading || routes.isLoading || vehicles.isLoading || territories.isLoading;
  const error = control.isError || routes.isError || vehicles.isError || territories.isError;
  const overloadLabel = settings.data?.overloadPolicy === "hard_block" ? text.hardBlock : settings.data?.overloadPolicy === "manager_override" ? text.managerOverride : text.warning;
  const reportRows = [{ label: text.routesToday, value: formatNumber(control.data?.routesToday ?? 0) }, { label: text.activeRoutes, value: formatNumber(control.data?.activeRoutes ?? 0) }, { label: text.loadedVehicles, value: formatNumber(control.data?.vehiclesLoaded ?? 0) }, { label: text.pendingDeliveries, value: formatNumber(control.data?.pendingDeliveries ?? 0) }, { label: text.collections, value: formatCurrency(control.data?.collections ?? 0) }, { label: text.returns, value: formatNumber(control.data?.returns ?? 0) }, { label: text.capacity, value: formatPercentage(control.data?.capacityUtilization ?? 0) }];
  const date = formatDate(new Date());
  const exportExcel = () => { try { setExporting("excel"); downloadDistributionExcel(text.title, date, reportRows, `nawa-distribution-${new Date().toISOString().slice(0, 10)}`); toast.success(text.exportReady); } catch { toast.error(text.exportFailed); } finally { setExporting(null); } };
  const exportPdf = async () => { try { setExporting("pdf"); await downloadDistributionPdf({ direction, title: text.title, date, documentLabel: text.eyebrow, amount: formatCurrency(control.data?.collections ?? 0), rows: reportRows, footer: text.description, fontFamily: "noto-arabic", paperSize: "A4" }, `nawa-distribution-${new Date().toISOString().slice(0, 10)}.pdf`); toast.success(text.exportReady); } catch { toast.error(text.exportFailed); } finally { setExporting(null); } };

  return <DashboardLayout><main dir={direction} className="mx-auto w-full max-w-7xl space-y-6 px-4 py-7 sm:px-6 lg:px-8">
    <section className="flex flex-col gap-4 rounded-2xl border border-primary/20 bg-gradient-to-br from-primary/15 via-card to-card p-6 shadow-sm">
      <div className="max-w-2xl"><p className="text-xs font-semibold tracking-[0.16em] text-primary">{text.eyebrow}</p><h1 className="mt-2 text-3xl font-bold tracking-tight">{text.title}</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">{text.description}</p></div>
      <div className="flex max-w-full flex-wrap gap-2"><Button asChild variant="outline" className="gap-2"><a href="/driver"><Smartphone className="h-4 w-4" />{text.driverWorkspace}</a></Button><Dialog open={vehicleOpen} onOpenChange={setVehicleOpen}><DialogTrigger asChild><Button variant="outline">{text.newVehicle}</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>{text.newVehicle}</DialogTitle><DialogDescription>{text.mobileWarehouse}</DialogDescription></DialogHeader><form className="grid gap-3" onSubmit={event => { event.preventDefault(); const data = new FormData(event.currentTarget); createVehicle.mutate({ code: String(data.get("code")), registrationNumber: String(data.get("registration")), type: String(data.get("type")), ownershipType: "owned", maximumPayloadWeight: Number(data.get("payload")), maximumVolume: Number(data.get("volume")) }); }}><Label>{text.vehicleCode}<Input name="code" required /></Label><Label>{text.registration}<Input name="registration" required /></Label><Label>{text.vehicleType}<Input name="type" required /></Label><Label>{text.payload}<Input name="payload" type="number" min="0" step="0.001" defaultValue="0" required /></Label><Label>{text.volume}<Input name="volume" type="number" min="0" step="0.001" defaultValue="0" required /></Label><Button type="submit" disabled={createVehicle.isPending}>{text.create}</Button></form></DialogContent></Dialog><Dialog open={territoryOpen} onOpenChange={setTerritoryOpen}><DialogTrigger asChild><Button variant="outline">{text.newTerritory}</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>{text.newTerritory}</DialogTitle></DialogHeader><form className="grid gap-3" onSubmit={event => { event.preventDefault(); const data = new FormData(event.currentTarget); createTerritory.mutate({ code: String(data.get("code")), name: String(data.get("name")) }); }}><Label>{text.territoryCode}<Input name="code" required /></Label><Label>{text.territoryName}<Input name="name" required /></Label><Button type="submit" disabled={createTerritory.isPending}>{text.create}</Button></form></DialogContent></Dialog><Dialog open={routeOpen} onOpenChange={setRouteOpen}><DialogTrigger asChild><Button variant="outline">{text.newRoute}</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>{text.newRoute}</DialogTitle></DialogHeader><form className="grid gap-3" onSubmit={event => { event.preventDefault(); const data = new FormData(event.currentTarget); createRoute.mutate({ routeDate: new Date(String(data.get("routeDate"))), stops: [] }); }}><Label>{text.routeDateInput}<Input name="routeDate" type="date" required /></Label><Button type="submit" disabled={createRoute.isPending}>{text.create}</Button></form></DialogContent></Dialog><Button variant="outline" onClick={exportExcel} disabled={loading || exporting !== null} className="gap-2"><Download className="h-4 w-4" />{text.exportExcel}</Button><Button variant="outline" onClick={() => void exportPdf()} disabled={loading || exporting !== null} className="gap-2"><FileText className="h-4 w-4" />{text.exportPdf}</Button><Button onClick={refresh} disabled={loading} className="gap-2"><RefreshCw className={`h-4 w-4 ${loading ? "animate-spin" : ""}`} />{text.refresh}</Button></div>
    </section>
    {error ? <Card className="border-destructive/30 bg-destructive/5"><CardContent className="flex items-center gap-3 p-5 text-sm text-destructive"><CircleAlert className="h-5 w-5" />{text.error}</CardContent></Card> : null}
    <section className="grid gap-4 sm:grid-cols-2 2xl:grid-cols-4">
      {loading ? Array.from({ length: 4 }).map((_, index) => <Skeleton key={index} className="h-28 rounded-xl" />) : <><Metric title={text.routesToday} value={formatNumber(control.data?.routesToday ?? 0)} icon={Route} /><Metric title={text.activeRoutes} value={formatNumber(control.data?.activeRoutes ?? 0)} icon={Activity} accent="text-emerald-500" /><Metric title={text.loadedVehicles} value={formatNumber(control.data?.vehiclesLoaded ?? 0)} icon={Truck} accent="text-amber-500" /><Metric title={text.pendingDeliveries} value={formatNumber(control.data?.pendingDeliveries ?? 0)} icon={MapPinned} accent="text-rose-500" /></>}
    </section>
    <section className="grid gap-4 lg:grid-cols-3">
      <Metric title={text.collections} value={formatCurrency(control.data?.collections ?? 0)} icon={Wallet} accent="text-emerald-500" />
      <Metric title={text.returns} value={formatNumber(control.data?.returns ?? 0)} icon={Activity} accent="text-rose-500" />
      <Metric title={text.capacity} value={formatPercentage(control.data?.capacityUtilization ?? 0)} icon={Truck} accent="text-primary" />
    </section>
    <section className="grid gap-6 xl:grid-cols-[1.35fr_1fr]">
      <Card><CardHeader><CardTitle>{text.operations}</CardTitle><CardDescription>{text.routeDate} · {text.status}</CardDescription></CardHeader><CardContent className="space-y-3">{routes.data?.length ? routes.data.slice(0, 7).map(item => <div key={item.id} className="flex items-center justify-between gap-4 rounded-xl border border-border/70 p-3"><div className="min-w-0"><p className="truncate font-medium">{item.routeNumber}</p><p className="mt-1 text-xs text-muted-foreground">{formatDate(item.routeDate)}</p></div><Badge variant={statusStyle[item.status] ?? "outline"}>{item.status}</Badge></div>) : <p className="py-8 text-center text-sm text-muted-foreground">{text.noRoutes}</p>}</CardContent></Card>
      <div className="space-y-6"><Card><CardHeader><CardTitle>{text.fleet}</CardTitle><CardDescription>{text.mobileWarehouse}</CardDescription></CardHeader><CardContent className="space-y-3">{vehicles.data?.length ? vehicles.data.slice(0, 4).map(vehicle => <div key={vehicle.id} className="rounded-xl border border-border/70 p-3"><div className="flex items-center justify-between gap-3"><p className="font-medium">{vehicle.code}</p><Badge variant={statusStyle[vehicle.status] ?? "outline"}>{vehicle.status}</Badge></div><p className="mt-1 text-xs text-muted-foreground">{vehicle.registrationNumber} · {text.capacityText}: {formatNumber(Number(vehicle.maximumPayloadWeight))}</p></div>) : <p className="py-5 text-center text-sm text-muted-foreground">{text.noVehicles}</p>}</CardContent></Card>
      <Card><CardHeader><CardTitle>{text.territories}</CardTitle><CardDescription>{text.settings}: {settings.data ? overloadLabel : "—"}</CardDescription></CardHeader><CardContent className="flex flex-wrap gap-2">{territories.data?.length ? territories.data.slice(0, 12).map(territory => <Badge key={territory.id} variant="secondary">{territory.code} · {territory.name}</Badge>) : <p className="py-2 text-sm text-muted-foreground">{text.noTerritories}</p>}</CardContent></Card></div>
    </section>
  </main></DashboardLayout>;
}
