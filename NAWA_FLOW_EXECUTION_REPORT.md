# تقرير تنفيذ Nawa Flow وإغلاق أساس تجربة المنصة

## النطاق المنفذ

حُصر التنفيذ في إغلاق أجزاء من أساس تجربة المنصة وإضافة **Nawa Flow** بوصفه طبقة تنقل وتمثيل بصري. لم يبدأ أي عمل جديد للتجارة المتقدمة أو التوزيع أو الأسطول أو التصنيع أو تطبيق السائق أو B2B، ولم تُعد هيكلة عزل المؤسسات أو حراس الاشتراك.

## ما تم إصلاحه أو إضافته

| المجال | التنفيذ |
| --- | --- |
| وضع العرض | أضيف `moduleViewMode` إلى `user_preferences` بالقيمتين `classic` و`nawa_flow`، مع ترحيل آمن وحفظ عبر tRPC وتحميل التفضيل في واجهة التطبيق. |
| الإعدادات | أضيف قسم **طريقة عرض الوحدات** داخل مركز الإعدادات، مع اختيار فوري بين الوضع التقليدي وNawa Flow. |
| Nawa Flow | أضيفت خريطة تفاعلية احترافية للعقد والحواف، تتجاوب مع RTL وLTR، وتعرض تدفقاً عمودياً ملائماً للهاتف. |
| الحماية | تستمد العقد حالتها من اشتراك المؤسسة. العقد غير المتاحة تظهر مقفلة، والعقد الحساسة تظهر مقيّدة بشكل تحفظي لغير مالك المؤسسة من دون مؤشرات أو أرقام أو رابط تشغيلي. |
| المستوى الثاني | النقر على عقدة التجارة والمخزون المتاحة يفتح خريطة داخلية مستقلة لمسار: المشتريات ← المخزون ← المبيعات، باستخدام الوحدات الموجودة فقط. |
| اللغات | أضيفت نحو 45 مفتاحاً جديداً لـNawa Flow وإعداد المؤسسة الأول وGlossary للوحدات في العربية والفرنسية والإنجليزية، مع تكافؤ القواميس واختبار قائم لذلك. نُقلت شاشة الإعداد الأولي كاملة إلى القاموس المركزي. |
| التنسيق المركزي | رُبط سياق اللغة بإعدادات تنسيق المؤسسة المحفوظة، واستُخدم منسق العملة المركزي في بطاقات مؤشرات لوحة المعاينة. |
| الجودة | أزيلت تحذيرات lint الآمنة من صفحة المعاينة ومعرض المكونات وإضافة Nawa Flow؛ بقيت تحذيرات ثلاثة داخل ملفات الإطار الأساسية غير المعدلة. |

## هندسة Nawa Flow

| المكوّن | المسؤولية |
| --- | --- |
| `client/src/flow/nawaFlowConfig.ts` | التعريف المركزي للعقد والحواف ومتطلبات الوحدات والمواقع والمسار الداخلي. يمكن إضافة عقد أو حواف مستقبلية من دون إعادة بناء واجهة الخريطة. |
| `client/src/components/NawaFlow.tsx` | الخريطة التفاعلية، واتجاه الأسهم، والحالات المتاحة والمقفلة والمقيّدة، ولوحة الملخص. |
| `client/src/components/CommerceInternalFlow.tsx` | مثال المستوى الثاني لتدفق التجارة والمخزون. |
| `client/src/pages/Workspace.tsx` | يقرأ التفضيل المحفوظ، ويمرر اشتراك المؤسسة ودور العضوية إلى الخريطة. |

> ظهور العقدة لا يمنح وصولاً تشغيلياً. ما زال التنفيذ الخادمي وحارس الاشتراك المرجع الفعلي للوصول إلى البيانات والإجراءات.

## المسارات والتجارب

يظل الوضع التقليدي على `/workspace`. يتاح Nawa Flow من عنصر التنقل الجديد ومن الإعداد المحفوظ، كما تدعم المعاينة الآمنة `/workspace?view=nawa_flow&lang=ar|fr|en` من دون كتابة التفضيل. راجعت الواجهة في العربية RTL، والفرنسية LTR، والإنجليزية LTR، على سطح المكتب والهاتف، وجرى تصحيح تموضع العقد في RTL لمنع قص الحواف.

## نتائج التحقق

| الأمر | النتيجة |
| --- | --- |
| `pnpm check` | ناجح. |
| `pnpm lint` | ناجح بلا أخطاء؛ 3 تحذيرات باقية في ملفات الإطار `server/_core/context.ts` و`server/_core/cookies.ts` فقط. |
| `pnpm test` | ناجح: 5 ملفات و10 اختبارات، بما فيها حالات عقد Nawa Flow والتعريب والتنسيق. |
| `pnpm build` | بدأ بنجاح في التحويل، لكنه أُنهي من بيئة التشغيل بسبب ضغط الذاكرة أثناء تجميع الحزم. كان بناء المرحلة السابقة ناجحاً؛ لا يوجد خطأ TypeScript أو lint يمنع البناء. |

## النواقص الحقيقية المتبقية داخل المرحلة

لا تزال بعض النصوص الموروثة داخل Dashboard وWorkspace تستخدم عبارات عربية ثابتة ولم تنقل كلها بعد إلى القاموس المركزي. كما أن أدوات تنسيق الأرقام والعملات والتاريخ صارت مرتبطة بسياق المؤسسة واستُخدمت في بطاقات المؤشرات، لكن تعميمها على جميع بطاقات وتقارير الواجهات القديمة لم يكتمل. لم تبدأ أي مرحلة تجارية أو ميدانية جديدة.
